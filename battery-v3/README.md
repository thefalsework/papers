# Battery v3 — the complete synthetic mechanism, taken to the field

**Question.** The oracle test (`accretion-study/05-oracle.mjs`) found
the *complete* mechanism of the strongest synthetic cell effect:
harmonic cone-membership mass, ORACLE(x) = Σ 1/|cone_200(u)| over
cones containing x — with it matched, the synthetic effect is exactly
zero. ORACLE is a pure graph feature, so it goes into the field
battery. Does anything survive?

**Verdict:** **Debian survives, unmoved** (+0.098 → +0.083 → +0.090
across batteries v1 → v2 → v3) — the strongest certification this
method can produce. **Go's E-over-D dissolves** (+0.152 → +0.018,
NULL), a registered miss scored as a miss, and Go exits the certified
table entirely: every Go effect ever certified is now explained by
theory-derived features. Registered record after this study: 9 for 26.

**What stands after the whole day:** exactly one claim, and it is the
sealed-bet one. Debian's Exploitation cell predicts future dependency
growth beyond in-degree, out-degree, age, exact graph distance,
PageRank, k-core, truncated transitive-dependent count, and harmonic
cone-mass — the last two supplied by the program's own generative
theory. No mechanism yet built produces the effect; no feature yet
derived deflates it.

**Files.**
- `lib.mjs` — the seventh feature: log1p(ORACLE), harmonic cone-membership mass.
- `01-precheck.mjs` / `precheck.json` — blind balance/feasibility; oracle imbalance audit of the v2 pairs.
- `02-batteryv3.mjs` / `results.json` — the registered run; verdicts and the state of the growth chapter in its postscript.
